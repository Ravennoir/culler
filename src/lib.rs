/// LightningView library crate.
///
/// The binary (`src/main.rs`) is the application entry point.
/// This library crate re-exports modules that need to be accessible from
/// integration tests (`tests/`) and benchmarks (`benches/`).
pub mod raw_preview;
pub mod eye_focus;
